# 小说节点快照 · 使用方法

把一篇 txt 小说交给 AI，先锁人物外貌，再按界面填写的固定分镜数拆出当时画面，并按选择生成 Danbooru tag、自然语言，或 Krea 2 二次元分镜简报。

自然语言模式固定拆成 **三次独立对话**（DeepSeek 或 Grok 都可以）：
1. 从小说提取人物一致性
2. 概括小说内容（不精简到丢掉结构；不同爱情姿势、不同爱情经过都要保留）
3. 根据「人物一致性 + 概括」再出 Anima 混合 TAG（英文：质量/人数/外观标签 + 短句；中文：逗号分隔的画面要素，只写看得见的外形和动作）

前两步会写成可改的中间稿。改完发色、姿势或场次后，不必重跑前两步，直接只跑第三步。

## 文件说明

| 文件 | 作用 |
|---|---|
| `start.bat` | Windows 一键打开可视化工具 |
| `start.sh` | macOS / Linux 一键打开可视化工具 |
| `snapshot_gui.py` | 可视化工具（推荐） |
| `novel_to_anima_system_prompt.txt` | Danbooru tag 模式的系统提示词（给 Anima） |
| `novel_to_nl_character_system_prompt.txt` | 自然语言第一步：人物一致性 |
| `novel_to_nl_summary_system_prompt.txt` | 自然语言第二步：详细故事概括 |
| `novel_to_nl_system_prompt.txt` | 自然语言第三步：根据档案和概括出 Anima 混合 TAG |
| `novel_to_krea2_system_prompt.txt` | Krea 2 二次元分镜简报的系统提示词 |
| `anima_snapshot.py` | 生成逻辑，GUI 和命令行共用 |
| `test_deepseek_novel_snapshot.py` | 命令行方式 |
| `samples/snapshot_test_excerpt.txt` | 测试用小说节选 |
| `outputs/` | 每次生成一个 `年月日时分秒` 文件夹 |
| `outputs/YYYYMMDD_HHMMSS/` | 本次三个阶段 md：`1_character.md`、`2_summary.md`、`3_tags.md`（Danbooru / Krea2 为 `snapshot.md`） |
| `test_output_deepseek.md` | 最近一次第三步/完整结果（会覆盖） |
| `gui_config.json` | 界面记住的地址、Key、模型、提示词类型、分镜数、性感服装开关、上次文件。本地生成，已加入 `.gitignore`，勿提交 |
| `gui_config.example.json` | 配置模板，不含 Key |

## 方式一：可视化工具（推荐）

先装好 Python 3。Windows 安装时勾选 **Add python.exe to PATH**，并保留 tcl/tk。

- Windows：双击 `start.bat`
- macOS / Linux：双击 `start.sh`，或在终端执行：

```bash
chmod +x start.sh
./start.sh
```

也可以手动启动：

```bash
cd /Users/wangbingbing/PycharmProjects/xiaoshuo_fenjing_tag
python3 snapshot_gui.py
```

界面操作：

1. 填写 **API 地址**
   - DeepSeek：`https://api.deepseek.com`
   - Grok / packyapi：`https://www.packyapi.ai/v1`（不要填成不带 `/v1` 的根域名）
2. 填写 **API Key**
3. 填写 **max_tokens**。DeepSeek 建议 `16384`；Grok 4.6 可更大
4. **API 协议**选 **自动** 即可：
   - `grok-4.6` 走 OpenAI **Responses**（`/v1/responses`）。它不支持 Chat Completions，硬走 `/chat/completions` 会报 `protocol_not_supported`
   - DeepSeek 仍走 **Chat Completions**
5. 点 **获取模型**，再在下拉框里选择模型
6. 选择 **提示词类型**：
   - **Danbooru**：逗号分隔的 Anima / Danbooru tag（默认），一次对话出完
   - **自然语言**：三次独立对话。前两步锁人设和概括；第三步中文是逗号分隔的画面要素（发、瞳、脸型、身材、服装、姿势），英文是 Anima 混合写法（标签锚定 + 短句）。不要写成小说描写。结果可改后再只跑第三步
   - **Krea2**：英文二次元分镜简报（先锁 2D 番剧赛璐璐，再写镜头、人和光），给 Krea 2 用。不要贴 Anima tag，也不要写成真人摄影
7. 在 **分镜数** 输入框填写正整数（1 到 100），生成正好这么多个节点
8. 需要更暴露的服装时勾选 **性感服装**：生成提示词时不跟故事原装，大胆猜想品类、颜色、暴露状态、花纹、样式、点缀。发色、身材、姿势、场次仍按正文。只跑第三步时也会读这个开关
9. 点 **选择文件**，选一篇 txt 小说
10. 点 **开始生成**
11. 自然语言生成后，到「人物一致性」「故事概括」页直接改稿，再点 **只跑第三步出 TAG**。不必重跑前两步，也不会再读小说原文
12. 完成后会自动打开生成的 md，也可点 **打开文件所在目录**。每次生成都会新建 `outputs/YYYYMMDD_HHMMSS/`，自然语言三步分别写入 `1_character.md`、`2_summary.md`、`3_tags.md`

设置会写到 `gui_config.json`，下次打开不用重填。Danbooru / Krea2 大约 1~3 分钟；自然语言三次对话大约 3~8 分钟。只跑第三步大约 1~3 分钟。

## 方式二：对话里直接用

1. 打开任意支持「系统提示词」的对话窗口（DeepSeek、ChatGPT、SillyTavern、本地客户端等）。
2. 按需要把对应系统提示词 **全文**粘贴到 System Prompt。Danbooru / Krea2 仍是一次对话；自然语言必须开 **三次新对话**，并把第三步里全部 `{{NODE_COUNT}}` 换成同一个正整数（1 到 100）。若不要性感服装，删掉 `{{CLOTHING_MODE}}` 这一行；若要更暴露的服装，把该占位符换成「服装不跟故事原装，大胆猜想更性感的品类、颜色、暴露状态、花纹、样式、点缀」。Grok 4.6 在对话客户端里一般没问题；本工具直连 packyapi 时必须走 Responses，不能当 DeepSeek 那样 POST `/chat/completions`：
   - Danbooru tag：`novel_to_anima_system_prompt.txt`
   - 自然语言第一步（人物一致性）：`novel_to_nl_character_system_prompt.txt`
   - 自然语言第二步（详细概括）：`novel_to_nl_summary_system_prompt.txt`
   - 自然语言第三步（Anima 混合 TAG）：`novel_to_nl_system_prompt.txt`
   - Krea 2：`novel_to_krea2_system_prompt.txt`
3. 用户消息不要寒暄。Danbooru / Krea2 最短模板（把 `12` 换成你要的固定分镜数）：

```text
请根据下面这篇 txt 小说正文，严格执行系统提示词。
本次必须输出正好 12 个关键节点，不多不少。必须先给角色一致性档案，再给节点。Krea2 模式还要先给全书视觉锁定。
不要寒暄，不要解释用法。

小说文件名：你的小说.txt

<novel>
（这里粘贴小说正文）
</novel>
```

自然语言请拆成三次新对话：

```text
# 对话 1 系统提示词：novel_to_nl_character_system_prompt.txt
# 用户消息：只贴小说，要求只出角色一致性档案

# 对话 2 系统提示词：novel_to_nl_summary_system_prompt.txt
# 用户消息：只贴同一篇小说，要求详细概括
# 不要过于精简；严格保留故事结构、不同爱情姿势、不同爱情经过

# 对话 3 系统提示词：novel_to_nl_system_prompt.txt（先把 {{NODE_COUNT}} 换成数字）
# 用户消息：只贴「对话 1 的档案」和「对话 2 的概括」，不要再贴原文
```

改过人设或概括后，不要重开前两步，只把改过的两份材料重新丢进对话 3。

4. 可选附加约束，写在小说前面即可，例如：
   - `只要女主相关节点`
   - `优先 NSFW 动作节点`
   - `全部用全身镜 full_body`
   - `只要陈子衣和苏清雅`
   - GUI 已勾选 **性感服装** 时不必再手写这条；手动粘贴时才需要写「服装不跟故事原装，大胆改成更暴露的一套」

5. 检查输出是否齐全：
   - 先有「角色一致性档案」
   - 再有正好你指定数量的节点
   - 每个节点都有「当时服装 / 当时姿态 / 中文提示词 / 英文提示词」
   - 自然语言第三步的英文是「标签块 + 空行 + 短句」，段首有 `masterpiece, best quality, score_7` 和 `1girl`/`1boy`，不是一篇超长从句。中文是逗号分隔外形词（圆脸、大胸、白皮肤），不是「憨痴惹人爱怜」「雪白细嫩」这类文艺句
   - 文末有「一致性自检」
   - 同一角色的发色、瞳色、体型在所有节点里没有换人

Danbooru 模式下，英文提示词那一行可以直接贴进 Anima。自然语言第三步也是给 Anima 用的：只复制「英文提示词」整块（质量/人数/外观标签 + 空行 + 短句），不要贴状态表。中文提示词可作对照，或直接用中文出图。Krea2 模式下，只复制「英文提示词」那一段贴进 Krea 2，不要贴中文、不要贴状态表、不要贴 Anima tag。Danbooru / Krea2 不要再加 `masterpiece`、`best_quality`、`highres`；自然语言第三步的英文混合块则必须自带 `masterpiece, best quality, score_7`。

小说太长时，先自行截一段（建议 8 千到 2 万字，覆盖换装和高潮），再整段贴进去。模型一次读不完整本长篇。

## 方式三：命令行脚本

DeepSeek 可用 `deepseek-v4-pro`、`deepseek-v4-flash`（不是旧的 `deepseek-chat`）。Grok 用 `grok-4.6`，协议必须是 Responses。

```bash
cd /Users/wangbingbing/PycharmProjects/xiaoshuo_fenjing_tag

export DEEPSEEK_API_KEY='你的key'

# 默认读取 samples/snapshot_test_excerpt.txt
python3 test_deepseek_novel_snapshot.py

# 指定小说路径（相对或绝对都可以）
python3 test_deepseek_novel_snapshot.py samples/snapshot_test_excerpt.txt
python3 test_deepseek_novel_snapshot.py /绝对路径/某本小说.txt

# 指定提示词类型：danbooru（默认）、natural 或 krea2
python3 test_deepseek_novel_snapshot.py 小说.txt --style danbooru
python3 test_deepseek_novel_snapshot.py 小说.txt --style natural
python3 test_deepseek_novel_snapshot.py 小说.txt --style krea2

# 指定固定分镜数（1 到 100，默认 12）
python3 test_deepseek_novel_snapshot.py 小说.txt --nodes 15

# 开启性感服装：不跟故事原装，大胆改成更暴露的一套
python3 test_deepseek_novel_snapshot.py 小说.txt --style natural --sexy-clothing

# Grok 4.6 / packyapi（自动按模型名走 Responses）
python3 test_deepseek_novel_snapshot.py 小说.txt --style natural --backend auto

# 自然语言：改过人物一致性或故事概括后，只重跑第三步
python3 test_deepseek_novel_snapshot.py 小说.txt --style natural --step 3
python3 test_deepseek_novel_snapshot.py 小说.txt --style natural --step 3 \
  --character outputs/20260921_143025/1_character.md \
  --summary outputs/20260921_143025/2_summary.md
```

路径支持 UTF-8 / GBK / GB18030。找不到文件会直接报错退出，不会改去读默认节选。

常用环境变量：

```bash
export DEEPSEEK_MODEL='deepseek-v4-pro'      # 或 deepseek-v4-flash / grok-4.6
export DEEPSEEK_MAX_TOKENS='16384'           # 低于 8192 时节点容易写到一半被截断
export DEEPSEEK_NOVEL_MAX_CHARS='20000'      # 超长小说只送开头；设 0 表示不截断
export DEEPSEEK_API_URL='https://api.deepseek.com'
export DEEPSEEK_PROMPT_STYLE='danbooru'      # 或 natural / krea2
export DEEPSEEK_NODE_COUNT='12'              # 固定分镜数，1 到 100
export SNAPSHOT_SEXY_CLOTHING='0'            # 1 开启性感服装，不跟故事原装
export SNAPSHOT_API_BACKEND='auto'           # auto / chat_completions / responses

# Grok 示例
export SNAPSHOT_API_URL='https://www.packyapi.ai/v1'
export SNAPSHOT_MODEL='grok-4.6'
export SNAPSHOT_API_KEY='你的 packyapi key'
```

结果写到 `outputs/YYYYMMDD_HHMMSS/`，并同步覆盖 `test_output_deepseek.md`。终端会打印结构校验：角色档案、节点数、中英文提示词块、是否误带质量 tag。

## 输出长什么样

Danbooru / Krea2 仍是一次对话出完，写入本次时间戳文件夹里的 `snapshot.md`。自然语言每次生成三个独立 md，不拼成一份：

1. **`1_character.md` 角色一致性档案**（第一步，也可人工改）  
   每个人一个 `C01 / C02 ...` 锁定卡：年龄、发、瞳、身材、胡子/眼镜等辨识点。自然语言锁中英文外貌句。没写到的发色、身材差、服装花纹要大胆推断拉开，一旦选定后面禁止改。
2. **`2_summary.md` 小说内容概括**（第二步，也可人工改）  
   保留整体故事结构；每一场爱情经过单独写；每换一种姿势就分条，不要合并成「两人做爱」。
3. **`3_tags.md` 节点目录 + 节点快照**（第三步）  
   正好 N 个节点。每个节点只冻一个可拍瞬间。服装走「最后已知状态」。中文输出连续画面描述；英文输出 Anima 混合块（标签锚定 + 短句），不要写成一篇英文散文。
4. **一致性自检**  
   核对锁定外貌有没有串台、不同姿势有没有被合并省略、服装链对不对。

Danbooru / Krea2 没有单独的概括文件：先角色档案，再节点目录，再节点快照，最后自检。Krea2 最前面还会多一段「全书视觉锁定」。

## DeepSeek 注意点

- Pro 默认会先「思考」。脚本已加 `"thinking": {"type": "disabled"}`。若你在网页/API 里自己调，也要关掉思考，否则 token 会烧光，正文可能是空的。
- 12 个完整节点大约要 8000~10000 completion tokens。`max_tokens` 建议 `16384`。自然语言三步会打三次，总耗时和总 token 都会更高。
- 改发色、改某场姿势后，只重跑第三步即可；第三步不再读小说原文，只认你改过的档案和概括。
- 本机若出现 SSL 证书校验失败，脚本已走未校验证书；网页版不受影响。
- Key 只用环境变量传入，不要写进仓库、不要写进这个 md。

## 常见问题

**想改人设或概括后再出 TAG**  
自然语言：在 GUI 的「人物一致性」「故事概括」页改稿，点「只跑第三步出 TAG」。命令行用 `--style natural --step 3`。不要重跑前两步，除非原文变了。

**输出数量对不上选定分镜数**  
正文太短、被 `max_tokens` 截断，或模型没按固定数量执行。换更长节选，加大 `max_tokens`，并确认系统提示词里的 `{{NODE_COUNT}}` 已被替换成同一个数字。

**写到第 8、9 个节点就停了**  
被 `max_tokens` 截断。加大上限，或在用户消息里加一句：`状态表写要点，必须一次写完全部节点和一致性自检`。

**同一人发色/胸型中途变了**  
把该角色档案里的锁定英文 tag / 锁定英文外貌复制出来，再让模型只重跑出错节点，并要求原样粘贴。

**画出来大家都长一个样**  
检查角色档案：发色、长短发、身材、胡子/眼镜是否人人错开。正文没写时系统提示词要求大胆补全并标「推断」，不要全员黑发精瘦。服装若只有「衬衫/裙子」，也不够；应有颜色+花纹+面料。

**英文出图糊、串人、空间乱**  
Anima 吃不了嵌套从句散文，也吃不了「憨痴惹人爱怜的甜相」「雪白细嫩」这种文艺词。第三步英文必须是混合写法：前面小写空格标签锁定人数和外观（`round face, large breasts, pale skin`），空一行后用短句写构图和动作。单人、双人直接写身份（`Young woman, lying on back`），不要 `On the left` / `On the right`。三人才写左右身份句：`On the left, boy` / `On the right, lean young man`，站位后立刻逗号，身份前不要 `a` / `an`。不要把锁定外貌整句塞进一个超长从句。中文也用逗号分隔外形词，可以直接贴进 Anima；英文优先贴混合块。

**英文 tag 不能出图**  
Danbooru 模式只复制「英文提示词」那一行。自然语言第三步只复制「英文提示词」整块（标签 + 短句），不要把状态表一起贴进 Anima。若英文仍是一篇超长从句、没有 `masterpiece` / `1girl` 前缀，说明没按混合写法输出，应重跑第三步。Krea 2 不要贴 Anima tag，改选 **Krea2** 后只复制英文二次元分镜简报。

**Krea 2 不听话、画面乱漂、不像小说**  
Anima 的 Danbooru tag（`1girl, BREAK, looking_at_viewer`）Krea 2 基本不吃。必须用 Krea2 模式：英文以 `2D anime still frame, modern TV anime cel, visible linework, cel-shaded...` 打头，写机位和赛璐璐光，不要写成真人摄影，不要堆 no crowd 否定词。出图只贴英文，80 到 160 词为宜。

**多人同框容易串台（眼镜戴错人、发色对调）**  
系统提示词已要求按角色分块绑定，仍无法保证百分百不串。检查输出时看：Danbooru 是否用了 `BREAK` 分角色块、独有特征是否只出现在对应块、三人是否有 `on_the_left` / `no_eyewear`；自然语言第三步英文是否按人分行写外观标签，双人短句是否用 the woman 回锚、三人是否用 on the left，文末是否有「只属于」；Krea2 是否一人一句、主语是否带「戴眼镜的白发男性」这类锚点。若仍串，优先拉开性别、发色、眼镜，或把第三人改成看不清脸。左右站位只给三人；单人、双人不要写 `on the left` / `on the right`。

**画出来多了一个人（两人变三人、三人变四人）**  
常见原因：提示词写了玻璃反光/镜子倒影、`married`/`丈夫`/`妻子`、走廊或办公室路人。单人、双人节点如果写了 `on the left` / `on the right` / `画面左侧`，Anima 会把人数算错。检查节点是否段首锁了「画面里只有 N 个人」、单人/双人是否误加了左右站位、公共场景是否写了空场、玻璃是否只写光斑不写倒影、锁定外貌句是否误带婚姻关系词。窗内一人窗外一人仍是两人，不要把倒影写成第三人。若仍多，把 `glass reflects` / `玻璃反光` 整句删掉；单人、双人把左右站位删掉。

**一镜人太多、出图崩脸/串台**  
每个分镜最多 3 个可识别人物。舞会、教室、家庭聚会不要一镜塞 4 人；应拆成连续几镜，或只留该瞬间最关键的 1～3 人，其余人不要出镜。检查「在场人物」列表是否超过 3 个 Cxx，Danbooru 是否出现了 `4girls` / `2girls, 2boys` / `multiple_girls`。

**多人画成合影（都看镜头、不对视）**  
检查机位是不是默认「正面平视并排」。多人应写成面对面、侧面、过肩，并写清谁看谁（对视双眼、盯着对方身体），禁止直视镜头 / `looking_at_viewer`。

**表情只有微笑、脸红，或乱加高潮脸**  
女性性爱按强度选：轻度潮红咬唇，中度愉悦表情/呻吟，高潮才用高潮脸、阿嘿颜、翻白眼、吐舌。男色欲场景用邪笑、得意、坏笑，不要一律微笑，也不要给熟睡/惊慌的男人加邪笑。

**想用别的模型**  
系统提示词本身不绑 DeepSeek。ChatGPT、Claude、本地模型都可以，只要支持长 System Prompt，并给够输出长度。
