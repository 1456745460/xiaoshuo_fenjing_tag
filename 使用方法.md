# 小说节点快照 · 使用方法

把一篇 txt 小说交给 AI，先锁人物外貌，再按界面下拉选定的固定数量（10 到 20）拆出当时画面，并按选择生成 Danbooru tag 或自然语言中英文提示词。

## 文件说明

| 文件 | 作用 |
|---|---|
| `snapshot_gui.py` | 可视化工具（推荐） |
| `novel_to_anima_system_prompt.txt` | Danbooru tag 模式的系统提示词 |
| `novel_to_nl_system_prompt.txt` | 自然语言模式的系统提示词 |
| `anima_snapshot.py` | 生成逻辑，GUI 和命令行共用 |
| `test_deepseek_novel_snapshot.py` | 命令行方式 |
| `samples/snapshot_test_excerpt.txt` | 测试用小说节选 |
| `outputs/` | 每次生成的 md |
| `test_output_deepseek.md` | 最近一次生成结果（会覆盖） |
| `gui_config.json` | 界面记住的地址、Key、模型、提示词类型、分镜数、上次文件。本地生成，已加入 `.gitignore`，勿提交 |
| `gui_config.example.json` | 配置模板，不含 Key |

## 方式一：可视化工具（推荐）

```bash
cd /Users/wangbingbing/PycharmProjects/xiaoshuo_fenjing_tag
python3 snapshot_gui.py
```

界面操作：

1. 填写 **API 地址**，例如 `https://api.deepseek.com`
2. 填写 **API Key**
3. 填写 **max_tokens**，建议 `16384`
4. 点 **获取模型**，再在下拉框里选择模型
5. 选择 **提示词类型**：
   - **Danbooru**：逗号分隔的 Anima / Danbooru tag（默认）
   - **自然语言**：完整中英画面描述，给自然语言文生图用
6. 在 **分镜数** 下拉框选择 10 到 20 的整数，生成正好这么多个节点
7. 点 **选择文件**，选一篇 txt 小说
8. 点 **开始生成**
9. 完成后会自动打开生成的 md，也可点 **打开文件所在目录**

设置会写到 `gui_config.json`，下次打开不用重填。生成中不要关窗口，一次大约 1~3 分钟。

## 方式二：对话里直接用

1. 打开任意支持「系统提示词」的对话窗口（DeepSeek、ChatGPT、SillyTavern、本地客户端等）。
2. 按需要把对应系统提示词 **全文**粘贴到 System Prompt，并把全部 `{{NODE_COUNT}}` 换成同一个 10 到 20 的整数，不要改其它结构：
   - Danbooru tag：`novel_to_anima_system_prompt.txt`
   - 自然语言：`novel_to_nl_system_prompt.txt`
3. 用户消息只给小说，不要寒暄。最短模板（把 `12` 换成你要的固定分镜数）：

```text
请根据下面这篇 txt 小说正文，严格执行系统提示词。
本次必须输出正好 12 个关键节点，不多不少。必须先给角色一致性档案，再给节点。
不要寒暄，不要解释用法。

小说文件名：你的小说.txt

<novel>
（这里粘贴小说正文）
</novel>
```

4. 可选附加约束，写在小说前面即可，例如：
   - `只要女主相关节点`
   - `优先 NSFW 动作节点`
   - `全部用全身镜 full_body`
   - `只要陈子衣和苏清雅`

5. 检查输出是否齐全：
   - 先有「角色一致性档案」
   - 再有正好你指定数量的节点
   - 每个节点都有「当时服装 / 当时姿态 / 中文提示词 / 英文提示词」
   - 文末有「一致性自检」
   - 同一角色的发色、瞳色、体型在所有节点里没有换人

Danbooru 模式下，英文提示词那一行可以直接贴进 Anima。自然语言模式下，复制整段中文或英文画面描述即可。不要再加 `masterpiece`、`best_quality`、`highres`。

小说太长时，先自行截一段（建议 8 千到 2 万字，覆盖换装和高潮），再整段贴进去。模型一次读不完整本长篇。

## 方式三：命令行脚本

当前可用模型是 `deepseek-v4-pro`、`deepseek-v4-flash`，不是旧的 `deepseek-chat`。

```bash
cd /Users/wangbingbing/PycharmProjects/xiaoshuo_fenjing_tag

export DEEPSEEK_API_KEY='你的key'

# 默认读取 samples/snapshot_test_excerpt.txt
python3 test_deepseek_novel_snapshot.py

# 指定小说路径（相对或绝对都可以）
python3 test_deepseek_novel_snapshot.py samples/snapshot_test_excerpt.txt
python3 test_deepseek_novel_snapshot.py /绝对路径/某本小说.txt

# 指定提示词类型：danbooru（默认）或 natural
python3 test_deepseek_novel_snapshot.py 小说.txt --style danbooru
python3 test_deepseek_novel_snapshot.py 小说.txt --style natural

# 指定固定分镜数（10 到 20，默认 12）
python3 test_deepseek_novel_snapshot.py 小说.txt --nodes 15
```

路径支持 UTF-8 / GBK / GB18030。找不到文件会直接报错退出，不会改去读默认节选。

常用环境变量：

```bash
export DEEPSEEK_MODEL='deepseek-v4-pro'      # 或 deepseek-v4-flash
export DEEPSEEK_MAX_TOKENS='16384'           # 低于 8192 时节点容易写到一半被截断
export DEEPSEEK_NOVEL_MAX_CHARS='20000'      # 超长小说只送开头；设 0 表示不截断
export DEEPSEEK_API_URL='https://api.deepseek.com/chat/completions'
export DEEPSEEK_PROMPT_STYLE='danbooru'      # 或 natural
export DEEPSEEK_NODE_COUNT='12'              # 固定分镜数，10 到 20
```

结果写到 `test_output_deepseek.md`。终端会打印结构校验：角色档案、节点数、中英文提示词块、是否误带质量 tag。

## 输出长什么样

固定四段，缺一段就重跑，不要手工补：

1. **角色一致性档案**  
   每个人一个 `C01 / C02 ...` 锁定卡：年龄、发、瞳、身材、胡子/眼镜等辨识点。Danbooru 模式锁中英文 tag；自然语言模式锁中英文外貌句。没写到的发色、身材差、服装花纹要大胆推断拉开，一旦选定后面禁止改。不同角色尽量不同发色、不同身材，避免画成同一张脸。
2. **节点目录**  
   正好 N 行（N 为选定分镜数）：节点号、短标题、在场人物、场景、服装变化要点。
3. **节点快照**  
   每个节点只冻一个可拍瞬间。服装走「最后已知状态」：正文没换装就继承；写了脱掉就必须跟上。每件衣服要有颜色、花纹、面料纹理，不能只写「白衬衫」。  
   Danbooru 模式输出逗号分隔 tag；自然语言模式输出连续中英画面描述。
4. **一致性自检**  
   核对锁定 tag 有没有串台、服装链对不对、哪些是推断。

## DeepSeek 注意点

- Pro 默认会先「思考」。脚本已加 `"thinking": {"type": "disabled"}`。若你在网页/API 里自己调，也要关掉思考，否则 token 会烧光，正文可能是空的。
- 12 个完整节点大约要 8000~10000 completion tokens。`max_tokens` 建议 `16384`。
- 本机若出现 SSL 证书校验失败，脚本已走未校验证书；网页版不受影响。
- Key 只用环境变量传入，不要写进仓库、不要写进这个 md。

## 常见问题

**输出数量对不上选定分镜数**  
正文太短、被 `max_tokens` 截断，或模型没按固定数量执行。换更长节选，加大 `max_tokens`，并确认系统提示词里的 `{{NODE_COUNT}}` 已被替换成同一个数字。

**写到第 8、9 个节点就停了**  
被 `max_tokens` 截断。加大上限，或在用户消息里加一句：`状态表写要点，必须一次写完全部节点和一致性自检`。

**同一人发色/胸型中途变了**  
把该角色档案里的锁定英文 tag / 锁定英文外貌复制出来，再让模型只重跑出错节点，并要求原样粘贴。

**画出来大家都长一个样**  
检查角色档案：发色、长短发、身材、胡子/眼镜是否人人错开。正文没写时系统提示词要求大胆补全并标「推断」，不要全员黑发精瘦。服装若只有「衬衫/裙子」，也不够；应有颜色+花纹+面料。

**英文 tag 不能出图**  
Danbooru 模式只复制「英文提示词」那一行。不要把状态表、原文锚点一起贴进 Anima。若目标模型吃自然语言，把界面改选「自然语言」再生成。

**多人同框容易串台（眼镜戴错人、发色对调）**  
系统提示词已要求按角色分块绑定，仍无法保证百分百不串。检查输出时看：Danbooru 是否用了 `BREAK` 分角色块、独有特征是否只出现在对应块、有没有 `on_the_left` / `no_eyewear`；自然语言是否一人一句、主语是否带「戴眼镜的白发男性」这类锚点。若仍串，优先拉开性别、发色、眼镜、左右位置，或把第三人改成看不清脸。

**画出来多了一个人（两人变三人、三人变四人）**  
常见原因：提示词写了玻璃反光/镜子倒影、`married`/`丈夫`/`妻子`、走廊或办公室路人。检查节点是否段首锁了「画面里只有 N 个人」、公共场景是否写了空场、玻璃是否只写光斑不写倒影、锁定外貌句是否误带婚姻关系词。窗内一人窗外一人仍是两人，不要把倒影写成第三人。若仍多，把 `glass reflects` / `玻璃反光` 整句删掉。

**多人画成合影（都看镜头、不对视）**  
检查机位是不是默认「正面平视并排」。多人应写成面对面、侧面、过肩，并写清谁看谁（对视双眼、盯着对方身体），禁止直视镜头 / `looking_at_viewer`。

**表情只有微笑、脸红，或乱加高潮脸**  
女性性爱按强度选：轻度潮红咬唇，中度愉悦表情/呻吟，高潮才用高潮脸、阿嘿颜、翻白眼、吐舌。男色欲场景用邪笑、得意、坏笑，不要一律微笑，也不要给熟睡/惊慌的男人加邪笑。

**想用别的模型**  
系统提示词本身不绑 DeepSeek。ChatGPT、Claude、本地模型都可以，只要支持长 System Prompt，并给够输出长度。
